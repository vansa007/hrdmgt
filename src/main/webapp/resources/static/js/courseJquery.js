$(document).ready(function() {
	
	jQuery('#modCouStartDate').datepicker({
		autoclose: true,
		todayHighlight: true,
		format: 'yyyy-mm-dd'
	});
	
	jQuery('#modCouEndDate').datepicker({
		autoclose: true,
		todayHighlight: true,
		format: 'yyyy-mm-dd'
	});
                
});